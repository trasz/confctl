double {
	slashed {
		// Test for handling "// comments"
		/var /val//comment
		no_val //comment
		slash_in_value foo / bar
		quoted_slash "foo / bar"
		squoted_slash 'foo / bar' // boring
		spaced foo / / bar
	}
}
